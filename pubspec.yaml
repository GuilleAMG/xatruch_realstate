name: xatruch_realstate
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.4.0
  cloud_firestore: ^6.1.2
  firebase_auth: ^6.1.4
  video_player: ^2.8.1
  url_launcher: ^6.3.0
  intl: ^0.19.0
  firebase_storage: ^13.0.0
  image_picker: any
  firebase_app_check: ^0.4.1+4
  local_auth: ^2.3.0
  firebase_messaging: any
  permission_handler: any
  google_maps_flutter: ^2.5.3
  geolocator: ^10.1.0
  purchases_flutter: ^9.15.0
  purchases_ui_flutter: ^9.15.0
  flutter_local_notifications: ^21.0.0
  firebase_crashlytics: ^5.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.4

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
